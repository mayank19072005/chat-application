<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <header class="mb-8">
            <div class="flex items-center justify-between">
                <h1 class="text-3xl font-bold text-indigo-600">Admin Dashboard</h1>
                <div class="flex space-x-3">
                    <a href="/chats" class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-4 rounded-lg shadow transition duration-300 ease-in-out flex items-center">
                        View All Messages
                    </a>
                    <form method="post" action="/admin/logout?token=<%= req.query.token %>" class="inline">
                        <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded-lg shadow transition duration-300 ease-in-out flex items-center">
                            Logout
                        </button>
                    </form>
                </div>
            </div>
        </header>
        
        <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Add New Task Message</h2>
            <form method="post" action="/admin/messages?token=<%= req.query.token %>" class="space-y-4">
                <div>
                    <label for="from" class="block text-sm font-medium text-gray-700 mb-1">From (Admin)</label>
                    <input id="from" type="text" placeholder="Admin" name="from" value="Admin" required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500">
                </div>
                
                <div>
                    <label for="to" class="block text-sm font-medium text-gray-700 mb-1">Assign To</label>
                    <input id="to" type="text" placeholder="Recipient name" name="to" required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500">
                </div>
                
                <div>
                    <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Task Message</label>
                    <textarea id="message" name="message" rows="4" required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500"
                        placeholder="Describe the task here..."></textarea>
                </div>
                
                <div class="flex justify-end">
                    <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white font-medium py-2 px-6 rounded-md shadow transition duration-300">
                        Add Task Message
                    </button>
                </div>
            </form>
        </div>
        
        <div class="bg-white rounded-lg shadow-lg p-6">
            <h2 class="text-xl font-semibold text-gray-800 mb-4">All Task Messages</h2>
            
            <% if (messages && messages.length > 0) { %>
                <div class="overflow-x-auto">
                    <table class="min-w-full bg-white">
                        <thead class="bg-gray-100">
                            <tr>
                                <th class="py-3 px-4 text-left text-sm font-semibold text-gray-700">To</th>
                                <th class="py-3 px-4 text-left text-sm font-semibold text-gray-700">Message</th>
                                <th class="py-3 px-4 text-left text-sm font-semibold text-gray-700">Date</th>
                                <th class="py-3 px-4 text-left text-sm font-semibold text-gray-700">Status</th>
                                <th class="py-3 px-4 text-left text-sm font-semibold text-gray-700">Actions</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-200">
                            <% messages.forEach(message => { %>
                                <tr class="hover:bg-gray-50">
                                    <td class="py-3 px-4 text-sm text-gray-800"><%= message.to %></td>
                                    <td class="py-3 px-4 text-sm text-gray-800"><%= message.message %></td>
                                    <td class="py-3 px-4 text-sm text-gray-500"><%= new Date(message.create_to).toLocaleString() %></td>
                                    <td class="py-3 px-4">
                                        <% if (message.completed) { %>
                                            <span class="px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">Completed</span>
                                        <% } else { %>
                                            <span class="px-2 py-1 text-xs font-semibold rounded-full bg-yellow-100 text-yellow-800">Pending</span>
                                        <% } %>
                                    </td>
                                    <td class="py-3 px-4 space-x-2 flex">
                                        <a href="/chats/<%= message._id %>/edit?token=<%= req.query.token %>&isAdmin=true" class="text-sm bg-indigo-100 hover:bg-indigo-200 text-indigo-700 py-1 px-2 rounded transition duration-300">
                                            Edit
                                        </a>
                                        <form method="post" action="/chats/<%= message._id %>?_method=Delete&token=<%= req.query.token %>" class="inline">
                                            <button type="submit" class="text-sm bg-red-100 hover:bg-red-200 text-red-700 py-1 px-2 rounded transition duration-300">
                                                Delete
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="text-center py-4">
                    <p class="text-gray-500">No task messages yet. Add your first task above!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>