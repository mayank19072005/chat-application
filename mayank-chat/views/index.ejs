<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <header class="mb-8">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <h1 class="text-3xl font-bold text-indigo-600">Messages</h1>
                    <a href="/admin/login" class="bg-gray-600 hover:bg-gray-700 text-white font-semibold py-1 px-3 rounded-lg shadow transition duration-300 ease-in-out flex items-center text-sm">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-6-3a2 2 0 11-4 0 2 2 0 014 0zm-2 4a5 5 0 00-4.546 2.916A5.986 5.986 0 0010 16a5.986 5.986 0 004.546-2.084A5 5 0 0010 11z" clip-rule="evenodd" />
                        </svg>
                        Admin
                    </a>
                </div>
                <div>
                    <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                        <div class="flex items-center space-x-3">
                            <span class="bg-green-100 text-green-800 text-xs font-semibold px-3 py-1 rounded-full">Admin Mode</span>
                            <a href="/admin/dashboard?token=<%= token %>" class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 px-4 rounded-lg shadow transition duration-300 ease-in-out flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M2 5a2 2 0 012-2h12a2 2 0 012 2v2a2 2 0 01-2 2H4a2 2 0 01-2-2V5zm14 1a1 1 0 11-2 0 1 1 0 012 0zM2 13a2 2 0 012-2h12a2 2 0 012 2v2a2 2 0 01-2 2H4a2 2 0 01-2-2v-2zm14 1a1 1 0 11-2 0 1 1 0 012 0z" clip-rule="evenodd" />
                                </svg>
                                Dashboard
                            </a>
                        </div>
                    <% } else { %>
                        <a href="/chats/new" class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 px-4 rounded-lg shadow transition duration-300 ease-in-out flex items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" clip-rule="evenodd" />
                            </svg>
                            New Message
                        </a>
                    <% } %>
                </div>
            </div>
        </header>
        
        <!-- Add search functionality -->
        <div class="mb-6">
            <form action="/chats/search<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>" method="GET" class="flex flex-col sm:flex-row gap-2">
                <div class="relative flex-grow">
                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </div>
                    <input 
                        type="text" 
                        name="q" 
                        placeholder="Search by name, recipient, or message..." 
                        class="block w-full pl-10 pr-3 py-2 border border-gray-300 rounded-md leading-5 bg-white placeholder-gray-500 focus:outline-none focus:placeholder-gray-400 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 sm:text-sm"
                        value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
                    >
                    <% if (typeof token !== 'undefined' && token) { %>
                        <input type="hidden" name="token" value="<%= token %>">
                    <% } %>
                </div>
                <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                    Search
                </button>
                <% if (typeof searchQuery !== 'undefined' && searchQuery) { %>
                    <a href="/chats<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>" class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md shadow-sm text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                        Clear
                    </a>
                <% } %>
            </form>
        </div>
        
        <% if (typeof searchQuery !== 'undefined' && searchQuery) { %>
            <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-5 w-5 text-blue-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <div class="ml-3">
                        <p class="text-sm text-blue-700">
                            Search results for: "<span class="font-medium"><%= searchQuery %></span>"
                            <% if (chats.length === 1) { %>
                                (1 result found)
                            <% } else { %>
                                (<%= chats.length %> results found)
                            <% } %>
                        </p>
                    </div>
                </div>
            </div>
        <% } %>
        
        <div class="space-y-4">
            <% for(let chat of chats) { %> 
                <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition duration-300 <%= chat.isAdmin ? 'border-l-4 border-indigo-600' : '' %>">
                    <div class="flex justify-between items-start mb-4">
                        <div>
                            <div class="flex items-center">
                                <div class="h-10 w-10 rounded-full <%= chat.isAdmin ? 'bg-indigo-700' : 'bg-indigo-500' %> flex items-center justify-center text-white font-bold mr-3">
                                    <%= chat.from.charAt(0).toUpperCase() %>
                                </div>
                                <div>
                                    <h3 class="font-semibold text-lg text-gray-800"><%= chat.from %> <%= chat.isAdmin ? '(Admin)' : '' %></h3>
                                    <p class="text-gray-500 text-sm">To: <span class="font-medium"><%= chat.to %></span></p>
                                </div>
                            </div>
                        </div>
                        <p class="text-xs text-gray-400">
                            <%= new Date(chat.create_to).toLocaleString() %>
                        </p>
                    </div>
                    
                    <div class="bg-indigo-50 rounded-lg p-4 mb-4">
                        <p class="text-gray-700"><%= chat.message %></p>
                    </div>
                    
                    <div class="flex justify-between items-center">
                        <div class="flex space-x-3">
                            <% if (chat.isAdmin) { %>
                                <form method="post" action="/chats/<%= chat._id %>/toggle-completed<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>" class="inline">
                                    <button type="submit" class="flex items-center text-sm <%= chat.completed ? 'bg-green-100 hover:bg-green-200 text-green-700' : 'bg-yellow-100 hover:bg-yellow-200 text-yellow-700' %> py-2 px-3 rounded transition duration-300">
                                        <% if (chat.completed) { %>
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                                            </svg>
                                            Completed
                                        <% } else { %>
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM7 9a1 1 0 000 2h6a1 1 0 100-2H7z" clip-rule="evenodd" />
                                            </svg>
                                            Mark as Completed
                                        <% } %>
                                    </button>
                                </form>
                            <% } %>
                            
                            <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                                <a href="/chats/<%= chat._id %>/edit<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>" class="text-sm bg-indigo-100 hover:bg-indigo-200 text-indigo-700 py-2 px-3 rounded transition duration-300 flex items-center">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                    </svg>
                                    Edit
                                </a>
                                
                                <form method="post" action="/chats/<%= chat._id %>?_method=Delete<%= typeof token !== 'undefined' && token ? '&token=' + token : '' %>" class="inline">
                                    <button type="submit" class="text-sm bg-red-100 hover:bg-red-200 text-red-700 py-2 px-3 rounded transition duration-300 flex items-center">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                        </svg>
                                        Delete
                                    </button>
                                </form>
                            <% } %>
                        </div>
                        <% if (chat.isAdmin) { %>
                            <div class="<%= chat.completed ? 'bg-green-100 text-green-800' : 'bg-yellow-100 text-yellow-800' %> px-3 py-1 rounded-full text-xs font-semibold">
                                <%= chat.completed ? 'Completed' : 'Pending' %>
                            </div>
                        <% } %>
                    </div>
                </div>
            <% } %>
            
            <% if (chats.length === 0) { %>
                <div class="text-center py-10">
                    <p class="text-gray-500">No messages yet. Start a new conversation!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
